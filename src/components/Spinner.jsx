import React from "react";
import spinner from "../img/spinner.svg";

const Spinner = () => {
  return (
    <div>
      <img src={spinner} alt="Preload" />
    </div>
  );
};

export default Spinner;